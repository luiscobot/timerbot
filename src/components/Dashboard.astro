---
import ImageDrop from "@/components/ImageDrop.astro";
import TimerDisplay from "@/components/TimerDisplay.astro";
---

<div class="dashboard">
    <TimerDisplay />

    <div class="controls">
        <button data-toggle class="control start">Iniciar</button>
        <button data-reset class="control reset">Reiniciar</button>
    </div>

    <ImageDrop />

    <audio id="timer-sound" preload="auto">
        <source src="/timer-complete.mp3" type="audio/mpeg" />
    </audio>
</div>

<style>
    .dashboard {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 48px;
    }

    .display {
        display: flex;
        align-items: center;
        gap: 6px;
    }

    .minutes,
    .seconds {
        position: relative;
        display: flex;
        flex-direction: column;
    }

    .digits {
        font-size: 12rem;
        line-height: 16rem;
        font-weight: 700;
        color: #fafafa;
        font-variant-numeric: tabular-nums;
    }

    .label {
        position: absolute;
        left: 0;
        right: 0;
        bottom: 0;
        width: 100%;
        font-size: 1rem;
        font-weight: 500;
        color: #52525b;
        text-align: center;
        text-transform: uppercase;
    }

    .separator {
        font-size: 6rem;
        color: #a1a1aa;
    }

    .controls {
        width: 100%;
        display: flex;
        justify-content: center;
        gap: 48px;
    }

    .control {
        width: 25%;
        padding: 18px 24px;
        border: none;
        border-radius: 64px;
        font-size: 1rem;
        font-weight: 500;
        color: #fafafa;
        cursor: pointer;
        transition: background-color 0.2s ease;
    }

    .control.start {
        background-color: #84cc16cc;
    }

    .control.start:hover {
        background-color: #84cc16;
    }

    .control.pause {
        background-color: #eab308cc;
    }

    .control.pause:hover {
        background-color: #eab308;
    }

    .control.reset {
        background-color: #0ea5e9cc;
    }

    .control.reset:hover {
        background-color: #0ea5e9;
    }
</style>
